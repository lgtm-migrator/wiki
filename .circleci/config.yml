version: 2
jobs:
  # build
  build:

    docker:

      - image: circleci/node:7.10
      
    working_directory: ~/repo

    steps:
      - checkout

      # Download and cache dependencies
      - restore_cache:
          keys:
          - v1-dependencies-{{ checksum "package.json" }}
          # fallback to using the latest cache if no exact match is found
          - v1-dependencies-

      - run:
          name: Install dependency
          command: npm install

      - run: 
          name: Build
          command: npm run build

      - store_artifacts:
          path: ~/repo/wikifile

      - add_ssh_keys:
          fingerprints:
            - "06:9a:b2:48:0c:33:a0:06:ee:61:27:37:49:e0:f8:b1"

      - run:
          name: Deploy
          command: |
            scp -rpC ~/repo/wikifile $SSH_USER@$SSH_HOST:sites

      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package.json" }}
